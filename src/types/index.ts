interface storage {
  navigationList: ['Main Menu', 'Liked'];
}

type storageItems = 'navigationList';
type storageValues = ['Main Menu', 'Liked'];

export {
  storage,
  storageItems,
  storageValues,
};
